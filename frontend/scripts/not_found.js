/* =========================================================
   NOT_FOUND.JS
   Это тупиковый экран "ничего не найдено" — реальный список и
   фильтрация живут в catalogue.html. Единственное осмысленное
   действие здесь — сбросить фильтры и вернуться в каталог.
   ========================================================= */

document.querySelector('.empty__btn').addEventListener('click', () => {
  window.location.href = 'catalogue.html';
});